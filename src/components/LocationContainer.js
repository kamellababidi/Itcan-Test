import React from 'react';

const LocationContainer = (props) => (
    <div className='location-container'>
        <img className='location-img' src={props.img}/>
    </div>
);

export default LocationContainer;
